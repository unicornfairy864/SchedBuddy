{
  "name": "@schedbuddy/server",
  "version": "0.2.6",
  "private": true,
  "description": "后端：Express + SQLite(better-sqlite3)，提供 API 与静态站点；可被 Electron 主进程内嵌，也可独立运行。",
  "main": "dist/index.cjs",
  "scripts": {
    "start": "node dist/index.cjs"
  },
  "dependencies": {
    "better-sqlite3": "^12.0.0",
    "express": "^4.21.2"
  }
}
