# 00 · 需求确认与决策记录

> 状态：生效　·　最近更新：v0.3.17（2026-09-08）　·　规范：`07-doc-standards.md`

> 本文件是**需求基线**：每次对话的疑问与结论都会同步到这里，实现以它为唯一口径。

## 0. 产品定位

帮助大学生把"脑子里的日程"可视化：
- 加入多个社团 → 单周/双周、隔 N 天、特定日期的社团活动；
- 主课 → 固定时间（不可变）；选修（水课）→ 可选上或不上；竞赛考试 → 临时一次性；
- 通过"周/日视图 + 时间轴横向条"看清每天重叠冲突，辅助决策（翘水课、考试提前交卷等）。

## 1. 已确认决策（v0.1.0 基线 · 含 2026-09-05 扩展）

> 行 1/4 已于 v0.2.28 扩展（三端形态 / 配对写授权），扩展后口径以 §5 决策日志为准。

| # | 问题 | 结论 |
| --- | --- | --- |
| 1 | 双端架构 | **A（已扩展为三端）**：桌面端 = 主机（内置后端+数据库，常驻运行）；局域网设备浏览器访问同一份数据（只读）；**规划新增 Android App 移动写端**（离线可用 + 手动同步，见行 8、§5） |
| 2 | 技术栈 | **A**：Electron + React + TS + Vite + Node(Express) + SQLite(better-sqlite3)，npm workspaces 单仓库、一份前端代码双端复用；**规划扩展：Android App 采用 React Native + TypeScript**（复用 shared 引擎） |
| 3 | 数据模型 | **A**：一个日程 = 一个条目，可含**多段时段**（周几/日期 + 起止），规则作用于整个条目 |
| 4 | 写入权限 | **A（已扩展）**：权威主机（本机）为第一写端；**已 PIN 配对的 App 为第二写端**；未配对局域网设备只读（v0.4 实现配对与 token 校验） |
| 5 | 规则范围 | 核心 3 规则（固定星期多段、单双周、隔 N 天）先行；同时纳入：**生效日期范围、重叠冲突检测与告警（可强制）、单次跳过/单次改期**（按序排队实现，均在 0.x 内落地） |
| 6 | 界面语言 | 简体中文（界面 + 文档 + 代码注释），周起始 = 周一 |
| 7 | 存储位置 | 全部在 `app/` 内：`app/data/schedbuddy.db` + 自动备份 + 导出/导入 JSON |
| 8 | 移动端形态（2026-09-05） | 新增第三端：**Android App（React Native + TS）**，解决"满课学生不回寝室、在校园临时收到社团/竞赛日程需当场新建/修改并查看"的场景；App 内置本地数据库，**离线也能看与改** |
| 9 | 网络范围（2026-09-05） | **仅局域网同步**：App 在离线（校园/移动网）状态下本地编辑，回到与主机同一 WiFi 后**由用户手动发起同步**；不做公网穿透，公网/口令鉴权列入 backlog |
| 10 | 同步模型（2026-09-05） | **离线优先 + 用户手动拉取/推送/合并（merge）**：pull 拉回主机变更；push 上传本地改动；两端同改同一条 → 不自动覆盖，进入合并列表由用户**逐条裁决**（保留主机版 / 保留本机版 / 手动编辑合并） |
| 11 | 同步数据基础（2026-09-05） | 记录级引入 **`rev` 单调递增同步序号（水印）**、**软删墓碑 `deleted_at`（删除走软删并双向传播）**、**`last_writer`（deviceId）**；`settings` 同样参与同步（**数据层已于 v0.2.29 落库**：迁移 v3 + DELETE 软删 + rev/last_writer 服务端维护；App 端 pull/push 消费属 v0.5） |
| 12 | 移动端写授权（2026-09-05） | 写权限判定 = **回环地址（本机）** 或 **携带有效设备 token（PIN 一次性配对换发）**；未配对局域网浏览器仍 403 只读 |

## 2. 四类日程（重要性从高到低）

| 类型 | type | 含义示例 | 重要度(越小越靠下/越先排) |
| --- | --- | --- | --- |
| 固定日程 | `fixed` | 数学课：每周一 8:00–9:30、周三 10:00–11:30 | 0（最先入排，通常落在最底排） |
| 特定日程 | `specific` | 社团活动：单周周二 18:00–20:00；隔 3 天训练 | 1 |
| 可选日程 | `optional` | 选修（水课） | 2 |
| 临时日程 | `temporary` | 竞赛考试（一次性某日某时） | 3 |

> 预留可扩展空间：类型、规则均为可扩展枚举 + JSON 参数，未来新增类别不改表结构（见数据模型文档）。

## 3. 字段要求（每个日程）

名称、备注、颜色（创建时选择）、时段列表（**多段**：每段 = 星期几或日期 + 开始 + 结束）、规则（见下）、生效日期范围（可选）、单次例外（后续）。

### 规则（已确认首批 + 待排期）

1. **固定星期多段** `weekly`：段列表按周重复，可叠加生效范围；
2. **单双周**：基于"学期第 1 周周一"锚点，标记 `odd` / `even`（与 weekly 组合）；若未配置学期起点则创建时提示填写；
3. **隔 N 天** `interval`：自起始日期起每 N 天发生一次；
4. **生效日期范围** `activeFrom/activeTo`：只在该范围（如第 1–16 周对应日期，或学期起止）内发生；
5. **重叠冲突检测与告警**：创建/编辑时列出重叠，按策略「拒绝 / 警告可强制」；
6. **单次跳过 / 单次改期**（occurrence override）：对具体某一次发生做例外，不改变规则本身。

### 我在规格外补充的设计决策（可推翻）

- **冲突策略**：两个 `fixed` 重叠 → 默认**拒绝**（可勾选强制保存）；其余任何组合重叠 → 黄色警告，允许强制保存。原因是"两门主课不能同时上"是硬约束，而社团/选修/考试冲突是用户要决策的对象。
- **颜色**：`fixed` 默认红 `#E4574E`，可在创建时修改；底部空闲带为浅绿；其余类型默认色 + 自由取色。
- **时段重叠判定**：`[开始, 结束)` 半开区间；同一日程自身重叠（同段）按错误提示。
- 每期对话递增版本并提交；大版本与依赖安装需授权（见 `05-versioning.md`）。

## 4. 范围边界（本期先做，后续对话扩展）

- 视图：**周 / 日**两种；月视图、学期总览、拖拽改期等列入 backlog。
- 桌面壳接入：源码可运行优先级 > 安装包分发。
- 写入主体（2026-09-05 扩展，覆盖原"本机唯一可写"）：**权威主机 = 本机**；已配对 App = 第二写端（离线可写、回网手动同步）；未配对局域网设备只读（原口径仅对浏览器成立）。

## 5. 决策日志（ADR-lite）

| 日期 | 决策 | 理由 | 影响 | 版本 |
| --- | --- | --- | --- | --- |
| 2026-09-04 | 排布不再为类型保留专用排：全部日程按重要度自底向上贪心入排，永不产生空排、同排可混类型 | 临时日程独占空排是缺陷 | packDay 重写；底排空闲仍画绿 | v0.2.16 |
| 2026-09-04 | 周视图 7 天共轴裁共有首尾空余，边界取整点且至多留 1h | 减少无信息空档 | weekRange 窗口；边界刻度保留 | v0.2.17–0.2.20 |
| 2026-09-04 | 日视图固定显示上午/下午两条半天带（半日无日程也显示） | 某半天有日程被误隐藏 | 半天带始终成对渲染 | v0.2.11 |
| 2026-09-04 | 方块间间隙改动 v0.2.21 作废回滚 | 用户验收不通过 | `git reset --hard` 至 v0.2.20 | 作废(0.2.21) |
| 2026-09-05 | v0.3 顶栏改「单行一体」分区：左=品牌（SchedBuddy，版本号平文置于名下方、字号稍小，无副题）＋周/日切换；中=日期导航（‹今天›+范围）；右=操作区；窄屏(≤860px，项目统一断点)右区折叠为 ⋯ 菜单；只读模式右区仅「只读」徽标（隐藏全部写入口） | 用户逐轮验收确定（原右侧版本胶囊与副题取消） | 顶栏重排；新增 Snackbar 轻提示；+新建/设置按钮下一验收轮接弹窗 | v0.2.23 |
| 2026-09-05 | 顶栏移动端适配（A+B 混合，迭代 v0.2.23 方案）：右区折叠阈值 860→900；新增档位 `≤720px` 折两行（品牌+周/日+右区 / 日期导航居中）、`≤480px` 极窄仅品牌 Logo（隐藏 SchedBuddy 与版本号）；板面内容断点仍 860 | 用户验收「大致可行」，先提交再微调 | 顶栏响应式三档；断点体系同步（06 §5 / 03 §7） | v0.2.24 |
| 2026-09-05 | **新增 Android App 移动写端**：React Native + TypeScript，直接复用 `shared` 引擎（time/expand/conflict/pack/validate）与 web 布局计算，UI 用 RN 组件重绘；双端架构扩展为"PC 权威主机 + 局域网只读 Web + 移动写端"三端 | 用户场景：满课学生不回寝室，需在校园用手机新建/修改社团、竞赛日程；Web 在手机只读显示已验证，移动端增量 = **可写 + 离线** | 形态/数据模型/API/里程碑重排（规划 v0.3–0.5）；Electron 桌面壳顺延 | v0.2.28（规划） |
| 2026-09-05 | **同步方案：仅局域网 + 离线优先 + 用户手动 pull/push/merge**；不做公网穿透；冲突不自动覆盖，由用户逐条裁决 | 学生在外（校园/移动网）连不上宿舍主机 → 需离线可写、回网收敛；单用户双写端冲突少、人工裁决可控 | App 内置库 = 离线工作区；数据模型引入 rev/墓碑/lastWriter；物理 DELETE 改软删（v0.3 起） | v0.2.28（规划） |
| 2026-09-05 | **移动写端授权 = PIN 一次性配对换发设备 token**；写判定 = 回环 或 携带有效 token；未配对 LAN 浏览器维持 403 只读 | 直接放开同网段写入会推翻原"局域网只读"安全决定；回环白名单又无法覆盖 App | server 新增配对端点与 token 校验中间件（v0.4 实现） | v0.2.28（规划） |
| 2026-09-05 | **路线重排**：先 v0.3（PC/Web 编辑核心 + 同步字段落库）→ v0.4（RN App 骨架：局域网直连 + 只读视图 + PIN 配对）→ v0.5（App 离线工作区 + 手动同步 + 冲突合并 + 删除传播）；原 v0.4 Electron 桌面壳顺延 | App 的编辑/校验/冲突能力依赖 v0.3 落地；移动端价值优先于桌面壳 | 里程碑表更新（`01-architecture.md` §5、`项目说明.md` §三） | v0.2.28（规划） |
| 2026-09-05 | **生效限制二选一**：日期范围 或 课程总数量（按天计次，同日多段=1 天） | 学生常按“一学期 N 次/天”而非起止日期管理课程 | 迁移 v4 `occurrence_limit`；引擎自起始沿时间轴累计 N 天截止（`docs/02` §3）；UI 高级区三档选择互斥 | v0.2.53 |
| 2026-09-05 | **v0.3.0 定版发布**：PC/Web 编辑核心里程碑归档（0.2.29–0.2.55 迭代），用户授权大版本号 | 里程碑完成按 `05-versioning.md` +1 minor | 版本 0.3.0 + CHANGELOG 归档 + 文档同步 | v0.3.0 |
| 2026-09-08 | **移动端视图形态定案**：只读周/日视图采用「纵向日卡列表」，**不移植** Web 的「横向时间轴 + packDay 排布」视觉 | 手机实机确认竖屏下列表式已适读；多排横向时间轴在竖屏信息密度与可点性差，移植成本高收益低 | 取消原 0.4.4 第 2 步；`03-ui-spec.md` 的横向时间轴口径标注为**仅适用 Web/桌面**；移动端仅复用 shared 展开/日期引擎 | v0.3.17 |
