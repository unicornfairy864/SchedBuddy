{
  "name": "@schedbuddy/desktop",
  "version": "0.2.22",
  "private": true,
  "description": "桌面壳：Electron 主进程随包 node.exe 子进程拉起 server（v0.3.5 起，better-sqlite3 单一系统 Node ABI 无需切换），Electron 只渲染窗口。主进程 src/main.ts（build.mjs desktop）；打包见 npm run dist:dir。",
  "main": "dist/main.cjs",
  "scripts": {
    "start": "electron ."
  }
}
